#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream out("input.txt");
    // out<<"This is a sample input file."<<endl;
    // out<<"It contains multiple lines of text."<<endl;

    ifstream in("input.txt");
    in>>

return 0;
}